spring:
  application:
    name: clients
  profiles:
    active: dev # Change this to use different remote configurations
  cloud:
    config:
      uri: http://localhost:8102
      allow-override: true