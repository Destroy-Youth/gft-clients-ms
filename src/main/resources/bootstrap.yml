spring:
  application:
    name: accounts
  profiles:
    active: ${ACTIVE_PROFILE:dev} # Change this to use different remote configurations
  cloud:
    config:
      uri: ${EUREKA_SERVER:http://localhost:8102}
      allow-override: true